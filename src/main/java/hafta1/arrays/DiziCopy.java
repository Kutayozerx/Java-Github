package hafta1.arrays;

import java.util.Arrays;

public class DiziCopy {
    public static void main(String[] args) {

/*

        int[] original = {1, 2, 3, 4, 5};
        int[] copy = original.clone();
        //Bu kod parçacığı, 'original' adlı bir diziyi 'copy' adlı bir diziye kopyalar.

        int[] original = {1, 2, 3, 4, 5};
        int[] copy = Arrays.copyOf(original, original.length);
        //Bu kod parçacığı, 'original' adlı bir diziyi 'copy' adlı bir diziye kopyalar.

        int[] original = {1, 2, 3, 4, 5};
        int[] copy = Arrays.copyOfRange(original, 1, 3);
        //Bu kod parçacığı, 'original' adlı bir dizinin belirli bir bölümünü (1. indeksten 3. indekse kadar) 'copy' adlı bir diziye kopyalar.

        int[] source = {1, 2, 3, 4, 5};
        int[] destination = new int[2];
        System.arraycopy(source, 1, destination, 0, 2);
        //Bu kod parçacığı, 'source' adlı bir dizinin belirli bir bölümünü (1. indeksten itibaren 2 eleman) 'destination' adlı bir diziye kopyalar.

        int[] source = new int[1000];
        int[] destination = new int[500];
        System.arraycopy(source, 250, destination, 0, 500);
        //Bu kod parçacığı, 'source' adlı büyük bir dizinin belirli bir bölümünü (250. indeksten itibaren 500 eleman) 'destination' adlı bir diziye kopyalar.

        int[] original = {1, 2, 3, 4, 5};
        int[] copy = original.clone();

        for (int i = 0; i < copy.length; i++) {
            System.out.println(copy[i]);
        }
        //Bu kod parçacığı, kopyalama işlemi sonrasında 'copy' adlı dizideki tüm elemanları yazdırır

        int[] original = {1, 2, 3, 4, 5};
        int[] copy = Arrays.copyOfRange(original, 1, 3);

        for (int i = 0; i < copy.length; i++) {
            System.out.println(copy[i]);
        }
        //Bu kod parçacığı, kopyalama işlemi sonrasında 'copy' adlı dizideki tüm elemanları yazdırır.

        int[] source = {1, 2, 3, 4, 5};
        int[] destination = {6, 7, 8, 9, 10};
        System.arraycopy(source, 1, destination, 2, 2);
        //Bu kod parçacığı, 'source' adlı bir dizinin belirli bir bölümünü (1. indeksten itibaren 2 eleman) 'destination' adlı bir dizinin belirli bir konumuna (2. indeks) kopyalar.

*/





    }
}
